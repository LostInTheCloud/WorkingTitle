# WorkingTitle
Dot Matrix Game Emulator

### Requirements:

```
sudo apt-get update
```
SDL2:
```
sudo apt-get install libsdl2-dev -y
```
Clang (++):
```
sudo apt-get install clang -y
sudo apt-get install clang++ -y
```

### To Compile and Run:

```bash
cmake .
make
./WorkingTitle
```
